---
scenario:
  name: rocky-linux-8
dependency:
  name: galaxy
driver:
  name: ec2
platforms:
  - name: ${BUILD_NUMBER}-pxc1-84-bootstrap-rl-8-install-${INSTALLTYPE}
    region: us-west-2
    image: ami-04310224db1f2a278
    vpc_subnet_id: subnet-03136d8c244f56036
    instance_type: t2.large
    ssh_user: rocky
    root_device_name: /dev/xvda
    instance_tags:
      iit-billing-tag: ${IIT_BILLING_TAG}
      job-name: ${JOB_NAME}
      build-number: ${BUILD_NUMBER}
provisioner:
  name: ansible
  playbooks:
    create: ../../../playbooks/create_noble.yml
    destroy: ../../../playbooks/destroy_noble.yml
    prepare: ../../../playbooks/prepare.yml
    converge: ../../playbooks/playbook.yml
    side_effect: ../../../../../playbooks/pxc84_upgrade_bootstrap.yml
  inventory:
    host_vars:
      ${BUILD_NUMBER}-pxc1-84-bootstrap-rl-8-install-${INSTALLTYPE}:
        ip_env: "{{ ansible_host }}"
