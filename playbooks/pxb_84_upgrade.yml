---
# This playbook does following:
#   sets main Percona repo
#   installs PS 8.0, PT and PXB 8.0
#   changes repo to testing
#   upgrades packages to the new version
#   does some tests
#

# Cosmic is still missing python
# - import_playbook: test_prep.yml

- hosts: all
  become: true
  become_method: sudo
  vars:
    gnupg_home: /root/.gnupg
    percona_key1: 4D1BB29D63D98E422B2113B19334A25F8507EFA5
    percona_key1_file: "{{ gnupg_home }}/PERCONA-PACKAGING-KEY"
  environment:
    PERCONA_TELEMETRY_URL: "https://check-dev.percona.com/v1/telemetry/GenericReport"

  tasks:
  - name: "[SETUP] Test environment setup"
    include_tasks: ../tasks/test_prep.yml

  - name: "[DEBUG-REPOS] Verify repos after PS main repo enable"
    include_tasks: ../tasks/debug_repos.yml

  - name: "[SETUP] Download world database"
    command: "{{ item }}"
    with_items:
    - wget --no-check-certificate -P /package-testing https://raw.githubusercontent.com/Percona-QA/percona-qa/master/sample_db/world.sql

# INSTALL PS MAIN REPO

  - name: "[PS-REPO] Enable PS 8.4 main repo"
    include_tasks: ../tasks/enable_ps84_testing_repo.yml

  - name: "[PS-INSTALL] Install Percona Server 8.4"
    include_tasks:
      file: ../tasks/install_ps84.yml
      apply:
        environment:
          PERCONA_TELEMETRY_DISABLE: "1"
    when:
      - lookup('env', 'server_to_test') == "ps-84"

  - name: "[SETUP] Install percona-release (ms-84)"
    include_tasks: ../tasks/install_percona-release.yml
    when:
      - lookup('env', 'server_to_test') == "ms-84"

# INSTALL MS (MAIN REPO)

  - name: "[SETUP] Extract PXB major and minor version"
    shell: cat /package-testing/VERSIONS | grep 'PXB84_VER' | cut -d'=' -f2 | sed 's/"//g'
    register: pxb_version_major_minor_register

  - name: "[SETUP] Extract PXB major version"
    shell: cat /package-testing/VERSIONS | grep PXB84_VER | awk -F'[=".]' '{print $3"."$4}'
    register: pxb_version_major_register

  - name: "[SETUP] Extract numeric major release version"
    shell: cat /package-testing/VERSIONS | grep -oP 'PXB84_VER="\K(\d+)\.(\d+)' | tr -d '.'
    register: major_release_version_register

  - name: "[SETUP] Set major_release_version fact"
    set_fact:
      major_release_version_reg: "{{ major_release_version_register.stdout }}"

  - name: "[SETUP] Set pxb_version_major fact"
    set_fact:
      pxb_version_major_reg: "{{ pxb_version_major_register.stdout }}"

  - name: "[SETUP] Set pxb_version_major_minor fact"
    set_fact:
      pxb_version_major_minor_reg: "{{ pxb_version_major_minor_register.stdout }}"

  - name: "[SETUP] Debug PXB version number"
    debug:
      msg: "PXB version number is {{ pxb_version_major_reg }} {{ pxb_version_major_minor_reg }}"

  - name: "[MS-INSTALL] Install MySQL MS Server"
    include_tasks: ../tasks/install_ms_innovation.yml
    vars:
      pxb_version_major: "{{ pxb_version_major_reg }}"
      pxb_version_major_minor: "{{ pxb_version_major_minor_reg }}"
    when:
      - lookup('env', 'server_to_test') == "ms-84"

  #### FOR AL2023 ######
  - name: "[PS-REPO] Enable PS 8.4 PRO repo (AL2023)"
    include_tasks: ../tasks/enable_ps84_pro_tools_repo.yml
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - ansible_facts['distribution_major_version'] | int == 2023

  - name: "[PS-INSTALL] Install Percona Server PRO 8.4 (AL2023)"
    include_tasks: ../tasks/install_ps84.yml
    vars:
      pro_suf: "{{ '-pro' if lookup('env', 'REPO_TYPE') == 'PRO' else '' }}"
    when:
      - lookup('env', 'REPO_TYPE') == 'PRO'
      - ansible_facts['distribution_major_version'] | int == 2023
##########

  - name: "[PXB-REPO] Enable PXB 8.4 release repo"
    command: percona-release enable pxb-84-lts release
    when:
      - lookup('env', 'REPO_TYPE') != "PRO"

  - name: "[DEBUG-REPOS] Verify repos after PXB release repo enable"
    include_tasks: ../tasks/debug_repos.yml

  - name: "[PXB-INSTALL] Install PXB 8.4 (main repo)"
    include_tasks:
      file: ../tasks/install_pxb84.yml
      apply:
        environment:
          PERCONA_TELEMETRY_DISABLE: "1"
    when:
      - lookup('env', 'REPO_TYPE') != "PRO"

  #####Enable and install PXB 80 PRO ###########
  - name: "[PXB-REPO] Enable PXB 8.4 PRO repo (release, RedHat)"
    include_tasks: ../tasks/enable_pxb84_pro_repo.yml
    vars:
      repo: "release"
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - lookup('env', 'server_to_test') == "ps-84"
      - ansible_facts['os_family'] == "RedHat"

  - name: "[PXB-REPO] Enable PXB 8.4 PRO repo (main, Debian)"
    include_tasks: ../tasks/enable_pxb84_pro_repo.yml
    vars:
      repo: "main"
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - lookup('env', 'server_to_test') == "ps-84"
      - ansible_facts['os_family'] == "Debian"

  - name: "[PXB-INSTALL] Install PXB 8.4 PRO (main repo)"
    include_tasks: ../tasks/install_pxb84_pro.yml
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - lookup('env', 'server_to_test') == "ps-84"
##############################################


## CHECK VERSIONS BEFORE UPGRADE
  - name: "[PXB-UPGRADE] Print PXB version before upgrade"
    shell: |
      {
        echo "XTRABACKUP VERSION is ";
        xtrabackup --version || echo "FAILED: xtrabackup --version";
        echo "xbstream VERSION is ";
        xbstream --version || echo "FAILED: xbstream --version";
        echo "xbcloud VERSION is ";
        xbcloud --version || echo "FAILED: xbcloud --version";
        echo "xbcrypt VERSION is ";
        xbcrypt --version || echo "FAILED: xbcrypt --version";
        echo "list rpm packages ";
        rpm -qa | grep "percona" || echo "FAILED: rpm -qa | grep percona";
        echo "list deb packages";
        dpkg -l | grep "percona" || echo "FAILED: dpkg -l | grep percona";
      }
    register: version_output
    ignore_errors: yes

  - name: "[PXB-UPGRADE] Display PXB version before upgrade"
    debug:
      var: version_output.stdout_lines

## Start mysql services

  - name: "[PS-START] Start mysqld service (RHEL)"
    command: systemctl start mysqld
    when: ansible_facts['os_family'] == "RedHat"

  - name: "[PS-START] Set root password (RedHat)"
    command: /package-testing/setpass_57.sh
    when: ansible_facts['os_family'] == "RedHat"

  - name: "[PS-START] Copy .my.cnf (RedHat)"
    template: src=../templates/my_57.j2
              dest=/root/.my.cnf
              mode=0640 owner=root group=root
    when: ansible_facts['os_family'] == "RedHat"

  - name: "[PS-START] Restart mysqld service (RedHat)
    service: name=mysqld state=restarted"
    when: ansible_facts['os_family'] == "RedHat"

  - name: "[PS-START] Restart mysql service (Debian)"
    service: name=mysql state=restarted
    when: ansible_facts['os_family'] == "Debian"

  - name: "[PXB-TEST] Install plugins and import world database"
    command: /package-testing/plugins_test.sh

  - name: "[SETUP] Install 3rd party packages (Debian)"
    apt:
      name: "{{ packages }}"
    vars:
      packages:
      - rsyslog-mysql
    when: ansible_facts['os_family'] == "Debian"

##
  - name: "[PXB-TEST] Check telemetry before upgrade (disabled)"
    command: /package-testing/check_telemetry.sh pxb -d
    when: lookup('env', 'REPO_TYPE') != "PRO"

  - name: "[PS-CLEANUP] Remove PXB rpm packages before upgrade"
    include_tasks: ../tasks/remove_pxb84.yml
    when: 
      - ansible_facts['os_family'] == "RedHat"

## ENABLE PXB REPOS NORMAL / PRO

  - name: "[PXB-REPO] Enable PXB 8.4 testing repo (upgrade)"
    command: percona-release enable-only pxb-84-lts testing
    when: lookup('env', 'REPO_TYPE') != "PRO"

  - name: "[PXB-INSTALL] Install PXB 8.4 (testing, upgrade)"
    include_tasks:
      file: ../tasks/install_pxb84.yml
      apply:
        environment:
          PERCONA_TELEMETRY_DISABLE: "1"
    when: lookup('env', 'REPO_TYPE') != "PRO"

  #####Enable and install PXB 80 PRO Testing ###########
  - name: "[PXB-REPO] Enable PXB 8.4 PRO testing repo"
    include_tasks: ../tasks/enable_pxb84_pro_repo.yml
    vars:
      repo: "testing"
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - lookup('env', 'server_to_test') == "ps-84"

  - name: "[PXB-INSTALL] Install PXB 8.4 PRO (testing)"
    include_tasks: ../tasks/install_pxb84_pro.yml
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"
      - lookup('env', 'server_to_test') == "ps-84"
##############################################

  - name: "[PXB-TEST] Check telemetry after upgrade (disabled)"
    command: /package-testing/check_telemetry.sh pxb -d
    when:
      - lookup('env', 'REPO_TYPE') !="PRO"
##
## VERSION CHECK

  - name: "[PXB-UPGRADE] Print PXB version after upgrade"
    shell: |
      {
        echo "XTRABACKUP VERSION is ";
        xtrabackup --version || echo "FAILED: xtrabackup --version";
        echo "xbstream VERSION is ";
        xbstream --version || echo "FAILED: xbstream --version";
        echo "xbcloud VERSION is ";
        xbcloud --version || echo "FAILED: xbcloud --version";
        echo "xbcrypt VERSION is ";
        xbcrypt --version || echo "FAILED: xbcrypt --version";
        echo "list rpm packages ";
        rpm -qa | grep "percona" || echo "FAILED: rpm -qa | grep percona";
        echo "list deb packages";
        dpkg -l | grep "percona" || echo "FAILED: dpkg -l | grep percona";
      }
    register: version_output
    ignore_errors: yes

  - name: "[PXB-UPGRADE] Display PXB version after upgrade"
    debug:
      var: version_output.stdout_lines

  - name: "[PXB-TEST] Check PXB version is correct (PRO)"
    command: /package-testing/version_check.sh pxb84 pro
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"

  - name: "[PXB-TEST] Check PXB version is correct"
    command: /package-testing/version_check.sh pxb84
    when:
      - lookup('env', 'REPO_TYPE') != "PRO"

  - name: "[PXB-TEST] Check PXB package versions are correct"
    command: /package-testing/package_check.sh pxb84
    when:
      - lookup('env', 'REPO_TYPE') != "PRO"

  - name: "[PXB-TEST] Check PXB package versions are correct (PRO)"
    command: /package-testing/package_check.sh pxb84 pro
    when:
      - lookup('env', 'REPO_TYPE') == "PRO"

  - name: "[PXB-TEST] Run backup after upgrade"
    command: /usr/bin/xtrabackup --backup --user=root --target-dir=/tmp/backups/

  - name: "[PXB-TEST] Prepare backup after upgrade"
    command: /usr/bin/xtrabackup --prepare --user=root --target-dir=/tmp/backups/

  - name: "[PS-CLEANUP] Remove Percona Server deb packages"
    apt:
      name: "{{ packages }}"
      update_cache: no
      state: absent
    vars:
      packages:
      - percona-server-server
      - percona-xtrabackup-84
    when: 
      - ansible_facts['os_family'] == "Debian"
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[PS-CLEANUP] Remove Percona Server rpm packages"
    dnf:
      name: "{{ packages }}"
      state: absent
    vars:
      packages:
      - percona-server-server
      - percona-xtrabackup-84
    when:
      - ansible_facts['distribution'] in ["RedHat", "CentOS", "OracleLinux", "Amazon", "Rocky"]
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[SETUP] Install iptables (RHEL)"
    dnf:
      name:
        - iptables
        - iptables-services
      state: present
    when:
      - ansible_facts['distribution'] in ["RedHat", "CentOS", "OracleLinux", "Amazon", "Rocky"]
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[SETUP] Enable and start iptables"
    systemd:
      name: iptables
      state: started
      enabled: true
    when:
      - ansible_facts['distribution'] == "RedHat"
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[SETUP] Block telemetry IP"
    iptables:
      chain: OUTPUT
      destination: check-dev.percona.com
      jump: DROP
    when:
      - lookup('env', 'upgrade_repo') == ""
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[PXB-INSTALL] Install PXB for telemetry"
    include_tasks: ../tasks/install_pxb84.yml
    when:
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[PXB-TEST] Check telemetry (uuid)"
    command: /package-testing/check_telemetry.sh pxb -u
    when:
      - lookup('env', 'REPO_TYPE') !="PRO"

  - name: "[PS-CLEANUP] Remove PXB packages for telemetry"
    include_tasks: ../tasks/remove_pxb84.yml
    when:
      - lookup('env', 'REPO_TYPE') !="PRO"
