# This task removes Percona Toolkit
#

  - name: remove Percona Toolkit new deb packages
    apt:
      name: "{{ packages }}"
      update_cache: yes
      state: absent
    vars:
      packages:
      - percona-toolkit
    when: ansible_os_family == "Debian"

  - name: remove Percona Toolkit new rpm packages yum
    yum:
      name: "{{ packages }}"
      state: absent
    vars:
      packages:
      - percona-toolkit
    when:
      - ansible_facts['distribution'] in ["RedHat", "CentOS", "OracleLinux", "Amazon"]
      - ansible_facts['distribution_major_version'] | int <= 7

  - name: remove Percona Toolkit new rpm packages dnf
    dnf:
      name: "{{ packages }}"
      state: absent
    vars:
      packages:
      - percona-toolkit
    when:
      - ansible_facts['distribution'] in ["RedHat", "CentOS", "OracleLinux", "Amazon"]
      - ansible_facts['distribution_major_version'] | int >= 8
