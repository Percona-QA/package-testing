- block:
    - shell: "sleep 1"
    - name: run pmm-admin list
      shell: "pmm-admin list xzc"
      register: last_list
  rescue:
    - name: pmm-admin list result in error
      debug: var=last_list
      failed_when: true

